const styles = [
    'color: white',
    'background: black',
    'font-size: 30px',
    'border-radius: 15px',
    'text-shadow: 2px 2px black',
    'padding: 10px',
  ].join(';'); // 2. Concatenate the individual array item and concatenate them into a string separated by a semi-colon (;)
  
  // 3. Pass the styles variable
  console.log('%cMade With 💖 By Healer#0088', styles);

  const style = [
    'color: blue',
    'background: white',
    'font-size: 30px',
    'border-radius:15px',
    'text-shadow: 2px 2px black',
    'padding: 10px',
  ].join(';'); // 2. Concatenate the individual array item and concatenate them into a string separated by a semi-colon (;)
  
  // 3. Pass the styles variable
  console.log('%cDont Mess With Console!', style);